# Changelog

All notable changes to this project will be documented in this file.

The format is based on [Keep a Changelog](https://keepachangelog.com/en/1.1.0/), and this project adheres to [Semantic Versioning](https://semver.org/spec/v2.0.0.html).

本仓库尚未发布版本，全部条目暂记在 `[Unreleased]` 下。每条标注日期与 PR（#N），可据此追到对应 commit。分支与 PR 的对应关系：`perf/optimization` → [PR #37](https://github.com/NJUPT-SAST/sast-link-backend-v2/pull/37)、`feat/oauth-grants-admin-stats` → [PR #41](https://github.com/NJUPT-SAST/sast-link-backend-v2/pull/41)、`feat/admin-delegated-scope` → [PR #44](https://github.com/NJUPT-SAST/sast-link-backend-v2/pull/44)（基于 #41）、`feat/user-scoped-access` → [PR #47](https://github.com/NJUPT-SAST/sast-link-backend-v2/pull/47)、`feat/stats-incomplete-buckets` → [PR #60](https://github.com/NJUPT-SAST/sast-link-backend-v2/pull/60)、`feat/admin-create-user` → [PR #61](https://github.com/NJUPT-SAST/sast-link-backend-v2/pull/61)、`feat/alumni-request` → [PR #66](https://github.com/NJUPT-SAST/sast-link-backend-v2/pull/66)、`feat/state-machine` → [PR #78](https://github.com/NJUPT-SAST/sast-link-backend-v2/pull/78)、`fix/incomplete-state-symmetry` → [PR #79](https://github.com/NJUPT-SAST/sast-link-backend-v2/pull/79)。正文里两处 `feat/oauth-scope` 是合并前未推送的暂定分支名，对应的改动均随 [PR #47](https://github.com/NJUPT-SAST/sast-link-backend-v2/pull/47) 落地。全部条目均已合入 `main`。

## [Unreleased]

### Added

- **工程基础设施**（2026-06-09 ~ 06-11）：仓库初始快照、PRD / API / OpenAPI / DB 设计文档交叉校验、CI 流水线、pre-commit、CONTRIBUTING、lint 配置、CI 最小权限。
- **HTTP 骨架**（2026-07-05，[PR #18](https://github.com/NJUPT-SAST/sast-link-backend-v2/pull/18)）：Go module、环境配置加载器、golangci-lint v2、响应信封 `{code, message, data}`、请求日志、Gin router、PostgreSQL / Redis 连接包、`GET /health`、API entrypoint。
- **数据基础 V001**（2026-07-19，[PR #20](https://github.com/NJUPT-SAST/sast-link-backend-v2/pull/20)）：V001 schema migrations 与 baseline guard、`cmd/migrate`（唯一 migration runner）、GORM 实体、Auth repository、Testcontainers（PostgreSQL 16）集成测试。
- **认证基础设施**（2026-07-22，[PR #22](https://github.com/NJUPT-SAST/sast-link-backend-v2/pull/22)）：PBKDF2 密码哈希、RS256 JWT/JWKS（active/previous kid 轮换）、opaque refresh token（HMAC-SHA256 存储）、PKCE-S256 原语、V002 强制 S256-only PKCE、OAuth scope 集中（`internal/scope`）、Redis 一次性状态 / JTI 黑名单 / 登录失败计数 / fixed-window 限流器。
- **内部会话闭环**（2026-07-26，[PR #23](https://github.com/NJUPT-SAST/sast-link-backend-v2/pull/23)）：密码登录、refresh rotation / replay 处理、登出、JWT middleware、`GET /user/profile`；V003 内置 first-party `sast-link-web` client seed、V004 token blacklist outbox（撤销事务写 outbox，worker 批量投递）；登录失败计数用 Lua 原子 INCR+PEXPIRE。
- **Redis 降级策略**（2026-07-26，[PR #25](https://github.com/NJUPT-SAST/sast-link-backend-v2/pull/25)）：health 把可选 Redis 报 `degraded` 而非 error；JTI 黑名单不可用时 middleware 回落 DB `revoked_at`；登录守卫与发码限流按策略 fail-open。
- **注册 / 密码 / 邮箱绑定**（2026-07-28，[PR #26](https://github.com/NJUPT-SAST/sast-link-backend-v2/pull/26)）：两步邮箱注册（Register-Ticket 一次性消费，`college` 必填 + 枚举校验）、忘记 / 重置密码（统一成功响应）、修改密码（`token_version` bump + 全量撤销，同事务）、第三方邮箱绑定（每用户 other_mail 上限 2）与解绑、V005 跨表邮箱唯一、SMTP mailer（收件人校验 + semaphore 限并发）。
- **资料自助服务**（2026-07-28，[PR #27](https://github.com/NJUPT-SAST/sast-link-backend-v2/pull/27)）：`PUT /user/profile`、`GET /card/:id`、`GET /user/identities`、`DELETE /user/identities/:id`（密码确认 + 唯一登录方式保护 + 按用户限流）。
- **OAuth 2.1 / OIDC Provider 与客户端管理**（2026-07-30，[PR #28](https://github.com/NJUPT-SAST/sast-link-backend-v2/pull/28)）：`GET /oauth/authorize` + `POST /oauth/authorize/consent` 两段式授权、token 端点（authorization_code + refresh_token，签发 ID Token）、RFC 7009 `POST /oauth/revoke`、`/userinfo`、discovery、JWKS；`/admin/oauth-clients` 客户端注册与更新（内置客户端受保护）；鉴权基础设施 `RequireRole`（从 DB 读角色）与 RFC 6750 `Authenticate` / `SetPrincipal`。
- **管理后台用户管理**（2026-07-31，[PR #29](https://github.com/NJUPT-SAST/sast-link-backend-v2/pull/29)）：`/admin/users` 分页列表 / 详情 / 更新 / 软删 / 恢复、`/admin/audit-logs` 审计日志查询；读写分级鉴权（列表与详情开放 lecturer，写操作仅 admin）；管理员自保护（不可改自己角色、不可注销自己）。
- **第三方 OAuth 登录**（2026-07-31，[PR #30](https://github.com/NJUPT-SAST/sast-link-backend-v2/pull/30)）：GitHub / 飞书授权跳转与回调、`login_code` 一次性交换（回调只投递 code）、登录态绑定、`registration_state` + `oauth_state` 双重校验的注册补全；飞书 `union_id` 作 `provider_id`、`tenant_key` 限 SAST 租户；compose 自包含本地栈；Caddy runbook。
- **端点限流**（2026-08-01，[PR #32](https://github.com/NJUPT-SAST/sast-link-backend-v2/pull/32)）：四个未限流端点补上限，`/auth/register/send-code` 按 Register-Ticket 配额而非调用方 IP；新增 `/auth/refresh` per-IP 限流。
- **数据保留 worker**（2026-08-01，[PR #33](https://github.com/NJUPT-SAST/sast-link-backend-v2/pull/33)）：过期授权码 / access / refresh 元数据 / 审计日志清理从 pg_cron 改为进程内 ticker worker，多实例用 PostgreSQL advisory lock 协调；V006 全量 `expires_at` 索引；retention 窗口可配置。
- **头像上传**（2026-08-03，[PR #34](https://github.com/NJUPT-SAST/sast-link-backend-v2/pull/34)）：`PUT /user/avatar`（≤5MB，jpg/png/webp 魔数 + 解码校验）、腾讯云 COS 存储、内容审核（fail-closed）、旧头像删除、按用户限流、审计 `upload_avatar`；`STORAGE_*` 配置；`internal/objectstore` ports + `internal/adapter/cos` 适配器。
- **设备管理**（2026-08-04，[PR #36](https://github.com/NJUPT-SAST/sast-link-backend-v2/pull/36)）：`GET /user/devices` + `DELETE /user/devices/:id`；设备记录存 Redis，`device_id` 复用 token `family_id`——设备生命周期与会话生命周期天然同步。密码 / 注册 / GitHub / Lark 登录均登记设备，任一会话终止路径（登出、登出指定设备、淘汰、改密、重置、刷新重放 / 轮换失败 / 过期、admin 降级 / 注销）同步清记录并审计（`logout_device` / `evict_device`）。5 台上限淘汰最旧设备并**真撤销其 family**（不沦为显示约束）；过期记录刷新时复活（受上限约束）；幻影成员（Hash 丢失）不占名额；设备记录 fail-open、`DeviceOwnedBy` 归属校验 fail-closed。Lua 原子原语（ZADD + HSET + 幻影清扫 + 淘汰）实现；review 修复：淘汰撤销延迟到轮换提交后（被判定死亡的刷新不误伤健康设备）、Hash 丢失重建时 `login_time` 从 ZSET score 还原。
- **压测工具**（[PR #37](https://github.com/NJUPT-SAST/sast-link-backend-v2/pull/37)，2026-08-04）：`scripts/loadmix/`（client/mix/burst/refresh/pool/stats）与 all-in-one bench 镜像。
- **授权应用管理**（[PR #41](https://github.com/NJUPT-SAST/sast-link-backend-v2/pull/41)，2026-08-09）：`GET /oauth/grants` + `DELETE /oauth/grants/:client_id`。用户可查看自己在同意页授权过的应用（每客户端取最近一次授权）并撤销其一：同一事务内撤销该 user×client 全部活跃 Access / Refresh Token 并失效 auth-state 缓存，再删除授权历史，应用从列表消失、下次使用必须重新同意；审计 `oauth_grant_revoke`（`resource = oauth`）。
- **同意页元数据**（[PR #41](https://github.com/NJUPT-SAST/sast-link-backend-v2/pull/41)，2026-08-09）：`GET /oauth/authorize/consent`。peek 暂存（GET + PTTL，不消费）返回服务端校验过的 `client_name` / `scopes` / `expires_in`，同意页展示值取自本端点而非可伪造的 consent URL 参数。按用户限流（`RATE_LIMIT_CONSENT_INFO_RPM`，默认 60/min），不用 IP——校园共享一个 NAT 出口 IP。
- **管理后台概览统计**（[PR #41](https://github.com/NJUPT-SAST/sast-link-backend-v2/pull/41)，2026-08-09）：`GET /admin/stats`，一次聚合账户（`total` / `by_role` / `by_state` / `by_department` / `no_department`）、客户端（`total` / `active`）与最近 5 条审计日志，供控制台概览页使用。软删除是状态位而非 `deleted_at` 列，故 `total` / `by_role` / `by_department` 只计未注销账户（`state ≠ is_deleted`），`by_state` 保留 `is_deleted` bucket 让注销数可见。
- **委派管理**（[PR #44](https://github.com/NJUPT-SAST/sast-link-backend-v2/pull/44)，2026-08-10）：第三方 token 可代管理员调用 `/admin/*`，这是全服务首个允许第三方 token 到达的内部端点组（后续 `/user` 自助面同样放行携带 `user:*` 的 token，见用户自助面条目）。新增 `admin:read` / `admin:write` 两个 scope，均不产生任何 OIDC claim（`scope.ClaimScopes` 在 ID Token signer 与 `/userinfo` 映射前滤掉，故 `openid admin:read` 拿到的字段与 `openid` 完全一致），仅可授予 `third_party` 客户端——`first_party` 是公开客户端，token 端点仅凭 PKCE 认证它，从授权码被签出到管理 token 存在之间只剩 `redirect_uri` 精确匹配一道屏障，机密客户端有两道。`/admin` 改由 `RequireAdminAuth` 认证（其余内部路由的 `azp` 门不变，仍一律拒绝第三方 token），每条路由显式叠加 scope 门与角色门，两者互不蕴含且缺一即 403：角色门读数据库行（降权下一请求生效），scope 门只约束委派调用（内置控制台 token 豁免）。路由经 `adminhandler.Gates` 结构体挂载，任一 gate 为 nil 则启动即 panic——新增路由不会因遗漏而继承权限。V007 新增 `audit_logs.actor_client_id` 记录行为主体（控制台操作显式记内置客户端 id 而非 NULL，使 NULL 保持「无 OAuth 凭证授权该操作」的单一含义）。接入方同时需要两种能力时用一个 `third_party` 机密客户端承载 `{openid profile email user:* admin:*}` + refresh 即可，均经控制台注册，迁移不种入任何接入方客户端。`profile` scope 另增 `role` claim，取自签发时的数据库行而非请求 token 的 role 快照，文档定位为展示提示而非授权依据。
- **委派管理的通用授予流程**（[PR #44](https://github.com/NJUPT-SAST/sast-link-backend-v2/pull/44)，2026-08-10）：控制台可为任意 `third_party` 客户端授予 admin scope，接入新运维工具不再需要改代码或写迁移。`adminclient.checkCapabilityScopeGrant` 在注册与更新两扇门上把守，缺一即拒：目标必须 `third_party`（400）；发起请求的凭证必须是内置控制台客户端（403，委派 token 不能授予或维护委派，否则委派集合会脱离运维批准自行增长）；不得与改写 `redirect_uris` 同请求完成（400）。均基于**合并后的状态**判定而非本次提交的字段。admin scope **允许 `refresh_token`**——`/admin` 的角色门从数据库行读取主体角色，刷新一个 admin-scoped token 不会拓宽谁能用它。
- **用户自助面**（[PR #47](https://github.com/NJUPT-SAST/sast-link-backend-v2/pull/47)）：`user:read` / `user:write` 两个 scope 让注册的客户端（如 SAST People，`third_party` 机密客户端）的 token 可访问 `/user/*`——token 以当前用户本人身份读/改自己的完整资料（含学号、手机号、学院、专业、QQ 等报名字段）、头像、邮箱身份绑定、改密码、登出、设备管理（GitHub/Lark 绑定仍仅内置控制台会话），无需经 `/admin/*`。`user:*` **无客户端类型约束**，任何客户端（含公开 `first_party`）皆可持有——`/user/*` 每个端点只操作 token 主体本人的记录，应用持有 user scope 不会是查他人凭据；**允许 `refresh_token`**（自助访问是会话）。四组能力 scope 均不产生任何 OIDC claim（`scope.ClaimScopes` 过滤全部非 OIDC scope）。`/user/*` 改由 `RequireUserAuth` 认证（内置控制台 token 豁免，会话行为不变），每条受保护路由显式挂 `user:read` / `user:write` scope 门（写蕴含读），经 `sessionhandler.Gates` 挂载。收窄或新授予 user scope 撤销该客户端存量 token，审计 `user_scope_granted` / `user_scope_revoked`。权限边界：user scope 的 token 的 `sub` 是用户本人，`/user/*` 只操作本人记录；`/user/*` 与 `/admin/*` 完全分离（user token 无 admin scope、admin token 无 user scope）。
- **能力 scope refresh family 生命周期封顶**（[PR #47](https://github.com/NJUPT-SAST/sast-link-backend-v2/pull/47)）：`JWT_REFRESH_CAPABILITY_MAX_LIFETIME`（默认 `168h`，0 关闭）为携带 `admin:*` / `user:*` 能力 scope 的 refresh family 设**总寿命上限**，从 family 首次授权起算——首个 refresh token 与每次轮换都按 `origin+cap` 夹紧，family 到点即撤销，客户端必须重新走授权，堵住「滑动 30 天窗口无限续期」的委托洞。普通 OIDC family 与内部会话 family 不受影响（滑动窗口原样保留），session 刷新路径完全不感知本功能。存量 pre-cap family 因 `ExpiresAt` 复检 + `created_at` 回读在到点后下一次刷新即被撤销，审计 `refresh_family_expired`（区别于 `refresh_replayed`）。
- **轮换 OAuth 客户端密钥**（[PR #47](https://github.com/NJUPT-SAST/sast-link-backend-v2/pull/47)）：`POST /admin/oauth-clients/:id/rotate-secret`。为机密（`third_party`）客户端重签 `client_secret`，仅 hash 入库、明文在响应中出现一次（`Cache-Control: no-store`）；存量 Access / Refresh Token 不受影响（从不依赖 secret），切断泄露的 secret 无需登出用户。公开客户端 `400`、非控制台发起 `403`，审计 `admin_oauth_client_rotate_secret`。
- **删除 OAuth 客户端**（[PR #47](https://github.com/NJUPT-SAST/sast-link-backend-v2/pull/47)）：`DELETE /admin/oauth-clients/:id`。物理删除注册：`oauth_clients` 行及其全部授权码 / Access / Refresh Token 元数据经 `ON DELETE CASCADE` 级联清除，删前同一事务内撤销活跃 token 并失效 auth-state 缓存，已签发 token 即刻全部失效、不可恢复。内置客户端（`INTERNAL_OAUTH_CLIENT_ID`）不可删除（`403`，防全站锁死）；带能力 scope 的客户端无额外限制——删除移除了凭据与其携带的 scope，控制台或委派管理员均可执行（与授予时「仅控制台」相对，防的是委派蔓延而非删减）。审计 `admin_oauth_client_delete`，拒绝路径同样落审计。
- **consent 提交与授权应用列表按用户限流**（[PR #47](https://github.com/NJUPT-SAST/sast-link-backend-v2/pull/47)）：consent 提交会铸授权码、`GET /DELETE /oauth/grants` 跑 family 撤销事务，各自拿到独立预算（`RATE_LIMIT_CONSENT_RPM` / `RATE_LIMIT_GRANTS_RPM`，默认 60/min），与 consent 预览一样按用户键控而非 per-IP（校园 NAT）；fail-open。
- **审计日志显示名**（[PR #41](https://github.com/NJUPT-SAST/sast-link-backend-v2/pull/41)，2026-08-09）：`GET /admin/audit-logs` 响应新增 best-effort `user_name`（按 `user_id` 批量回查显示名；软删除的行仍在表里、名字照常返回，仅物理删除或回查失败时为 `null`）。
- **新开标签页会话引导：httpOnly session cookie**（2026-08-13，[PR #49](https://github.com/NJUPT-SAST/sast-link-backend-v2/pull/49)）：登录 / 注册 / 第三方登录兑换 / 刷新时下发 `sl_session` cookie（值即轮换中的 refresh token，HttpOnly + host-only + SameSite=Lax + Secure 默认，Path/名称可配，Max-Age 跟随 token 剩余寿命），新 tab 调 `POST /auth/refresh`（空 body `{}`）时由 cookie 充当 refresh 凭证换取 token pair。新增 errcode `40108` 并发刷新冲突（30s 宽限窗内被兄弟请求轮换，家族保留）：审计 outcome 拆为 `concurrent_refresh` / `refresh_replayed`（session 与 `/oauth/token` 两条刷新路径一致），客户端应重读 cookie 重试一次。登出 / 改密 / 重置密码时清 cookie；刷新失败仅在凭证来自 cookie 且家族已死（40102/40301）时清。compose 本地栈显式覆盖 `SESSION_COOKIE_PATH=/` + `SESSION_COOKIE_SECURE=false`（root 服务 + 纯 HTTP 下生产默认值会让该功能静默失效）。
- **概览统计的未补全分桶**（[PR #60](https://github.com/NJUPT-SAST/sast-link-backend-v2/pull/60)）：`GET /admin/stats` 的账户聚合新增 `incomplete_by_role` / `incomplete_by_state`（不新增端点，现有字段不变），供控制台概览把 V010 标记（`profile_needs_completion`）的迁移残留账户单独归为「未补全」扇区。口径：`incomplete_by_role` 只计未补全且角色 ∉ {`lecturer`, `admin`} 的账户（按角色分组），`incomplete_by_state` 只计未补全且状态 = `njupter` 的账户（按状态分组）——讲师 / 管理员角色与 `on_sast` / `retired_sast` 状态视为组织内成员，即使资料未补全也不列为待跟进对象。两个桶同样只计未注销账户，且均为 `by_role` / `by_state` 的子集而非独立桶（前端从真实桶减去后合并为一个扇区，分母 `total` 不变）。
- **管理员建号与个人邮箱密码重置**（[PR #61](https://github.com/NJUPT-SAST/sast-link-backend-v2/pull/61)）：`POST /admin/users` 为无法自助注册的人员建号（如已毕业成员）。`login_email` 限注册白名单域名；可选 `personal_email` 在同一事务内直绑为 `other_mail` 登录身份，无需邮箱验证，绑定后可用于登录和密码重置。系统生成 32 位随机初始密码，仅在本次响应中返回，不落库也不入审计。`login_email`、`student_id` 或绑定邮箱重复时返回 `409`，不静默复用旧账号。忘记 / 重置密码（`POST /auth/forgot-password/send-code`、`POST /auth/reset-password`）改为按登录标识解析账号，支持已绑定的 `other_mail` 个人邮箱。审计新增 `admin_user_create`。
- **校友建号工单化**（[PR #66](https://github.com/NJUPT-SAST/sast-link-backend-v2/pull/66)）：已毕业成员没有 `@sast.fun` 邮箱、学生邮箱也已停用，`/auth/register` 第一步的域名白名单会阻止其自助注册；`POST /admin/users` 能建号但校友侧没有入口去「请求建号」。V011 新增 `alumni_requests` 工单表与五个端点：`POST /alumni-requests`（免登录）、`GET /admin/alumni-requests`、`GET /admin/alumni-requests/:id`、`POST /admin/alumni-requests/:id/approve`、`.../reject`、`.../resend-notification`。**身份核验仍是人工的**——工单化自动的是转录劳动，不是核验责任：学号加姓名在毕业生群体里不是秘密，且 SMTP 发件人可伪造，「收到邮件即自动通过」等于给任意人开号能力，故邮件解析方案被否（`internal/mailer` 只有出站 SMTP）。工单收两个邮箱：`login_email` 是原学号邮箱（仍限白名单域名，V001 触发器 `auto_set_email_type` 依赖它派生 `email_type`），`personal_email` 是可收信的第三方邮箱，审批后在同一事务内直绑为 `other_mail` 身份，也是通知与自助改密的收件地址——发往 `login_email` 等于不发。审批是**一个事务**做三件事：锁工单行（`SELECT ... FOR UPDATE`）→ 建号 → 回写结果，任一步失败整体回滚、工单留 `pending`；拆开会产生两种中间态，「账号已建 + 工单仍 pending」会让重试撞上自己刚插入的学号唯一索引，「工单已判 + 账号没建」会让校友拿着通过邮件去登录一个不存在的账号。控制台按钮双击时第二个事务读到已 `approved`，返回 `42204` 而非重复建号。**不返回初始密码**：生成后哈希即丢弃，校友走 `/reset` 自助设置（`ApproveResult` 是独立类型而非复用带 `InitialPassword` 字段的 `adminuser.CreateUserResult`，`mailer.AlumniResult` 同样无该字段——让「不发密码」成为类型的性质而不是调用方的习惯）。审批与驳回都自动发件，`notify_attempts` 在投递**之前**递增、`notified_at` 仅在 SMTP 接受后写入，故「已增未确认」读作真实状态；配 `notified=true|false` 筛选与手动重发端点，因为通过邮件是校友唯一的「去设置密码」指引，丢了等于建号白做。校验规则**比管理员建号更严**：`major` 必填、`name` 不能等于 `student_id`——两者正是 V010 生成列判定资料残缺的两种形态，放宽会让新号一登录就被赶去资料补全页；`name` 判据复用 `validate.IncompleteProfileFields` 而非本地重写，与 SQL 同源。新增 errcode `40021`（人机校验未通过）、`40403`（申请不存在）、`40906`（该学号已有待审申请）、`42204`（申请已被处理）、`50301`（申请通道不可用）；邮箱占用**复用 `40901`** 而不新增码——客户端处理方式完全相同，一个可观测结果两个码就是漂移的开始。审计新增 `alumni_request_submit`（`user_id` 与 `actor_client_id` 均为 NULL：未认证提交，没有凭证授权它）/ `_approve` / `_reject` / `_resend_notification`，通过审批额外补一条 `admin_user_create`（resource `user`，detail 带 `via` 与工单 id）——审批不走 `adminuser.CreateUser`，否则审计里会出现「无人创建的账号」。
- **Cloudflare Turnstile 人机校验**（[PR #66](https://github.com/NJUPT-SAST/sast-link-backend-v2/pull/66)）：`internal/adapter/turnstile` 接入 siteverify（5s 超时、校验 `action`、不重试）。`ErrFailed` 与 `ErrUnavailable` 严格分开，二者是相反的指令：前者「重新完成验证后可重试」映射 `40021`/400，后者「重做验证无意义」映射 `50301`/503，客户端据此隐藏申请入口——非 200、超时、响应无法解析都归后者。**不重试**：token 一次性，超时后无从知道 Cloudflare 是否已消费它，重试很可能撞 `timeout-or-duplicate`，把网络抖动报成用户没通过验证。未配置 `TURNSTILE_SECRET` 时注入**永远拒绝**的实现而非 nil——nil 依赖要靠每个调用点判空，漏一处就是未验证的匿名写入。校验器是服务层 port 而非中间件：判定结果要进同一条审计记录，且必须在字段校验**之后**运行（中间件必然早于 handler 解 body，而先烧 token 会让申请人改一个错字就得重过一次验证）。字段规则本就公开在前端，前置不新增泄露面；真正的泄露面是邮箱与学号的占用查询，它们留在人机校验与限流之后。新增 `TURNSTILE_SECRET` / `TURNSTILE_ACTION` / `TURNSTILE_TIMEOUT` / `ALUMNI_RESET_URL` / `ALUMNI_SUPPORT_EMAIL` / `RATE_LIMIT_ALUMNI_REQUEST_RPM` / `_WINDOW`（整组可选，空 secret 不等于跳过校验，而是端点一律拒绝——匿名写端点关掉挑战只剩限流兜底）。
- **校友通道缺口修复**（2026-08-27，fix/alumni-gaps 分支）：四项审查发现。
  - **学号大小写撕裂**：提交预检与审批用敏感比较，而 V011 待审索引折叠、V001 `student_id UNIQUE` 敏感——库里已有 `b24040525` 时提交/批准 `B24040525` 会平白多出第二个账号（import 恰好产出过这种形态）。占用检查与审批事务内复查均改 `lower(btrim(student_id))`；schema 层终态唯一索引待生产数据审计后另行决定。
  - **管理员补绑个人邮箱**：`PUT /admin/users/:id` 新增可选 `personal_email`，同一事务内直绑为现有账号的 `other_mail` 身份（W 救援路径：每账号 ≤2 的 V001 触发器与 V005 触发器照常收口，撞限返回已分类的 `40905` 而非裸 P0001）。
  - **同邮箱多张待审单拦截**：提交端拒绝对「已有 pending 工单的地址」再排一张单（409 复用 `40906`）；驳回后放行，首个批准仍绑走地址是其余工单必然卡死的源头，提前挡在提交时刻。
  - **通知队列重启自愈**：通知队列仍是进程内有界 channel，但 worker 启动时重扫 `notified_at IS NULL AND notify_attempts = 0` 的已审工单重新入队——进程重启不再丢未发送通知；计数职责完整留在发送处，不会双计。
- **恢复访问工单化**（2026-08-27，fix/alumni-gaps 分支）：V013 为 `alumni_requests` 加 `intent` 列（缺省 `provision`，旧行行为不变）。`recover` 工单不开新号：审批在同一事务内按折叠学号定位并锁定现有账号、重验 `login_email` 一致性，把申请中的个人邮箱直绑为该账号的 `other_mail` 身份——解决「账号还在、学校邮箱停用、从未绑定过任何可收信邮箱」人群的死路（此前被 provision 占用检查档在门外且无自救路径：重置码发不到死邮箱，绑定新邮箱又要登录态）。技术护栏：先锁工单行再锁 user 行（与其它写锁序不成环）、目标已注销返回 `42200`、绑定上限返回 `40905`；已有第三方绑定一概不动（解绑永远需要密码），审计单行记 `recovered/target_user_id/bound_email` 且**不发**开户行。通知链路加 `Recovered` 标志选「访问方式已恢复」文案；通知自愈同样按 intent 选文案。学号占用的 `409` 文案改指向本流程。新增字段见 API 文档 §6.13 与 openapi；**前端需把 recover 卡片按高危操作呈现**（展示目标账号现有资料与绑定），人工核验下限不变。

### Changed

- **能力 scope refresh family 生命周期封顶的配套修正**（[PR #47](https://github.com/NJUPT-SAST/sast-link-backend-v2/pull/47)）：consent 提交限流（`RATE_LIMIT_CONSENT_RPM`）**只对 approve 路径计费**——`approve: false` 的拒绝不铸码、不消耗配额，且被限流的 approve 在消费暂存之前返回 `42900`，可用同一 `request_id` 重试。grants 列表与撤销拆成**独立预算**（`oauth_grants_list` / `oauth_grants_revoke`），读列表耗尽不了撤销的配额；两者超限现在返回 `429` + `Retry-After`（此前被 handler 折叠成 500，与 consent 不一致）。`adminclient.mergedRegistration` 不再静默吞 `scope.Normalize` 失败，守卫不再可能基于错误的合并状态做授权决定。
- **rotate-secret 拒绝路径补审计**（[PR #47](https://github.com/NJUPT-SAST/sast-link-backend-v2/pull/47)）：`POST /admin/oauth-clients/:id/rotate-secret` 的公开客户端 `400`、非控制台 `403`、未知 id `404` 与写库失败现在都落审计（`admin_oauth_client_rotate_secret`，success=false + 错误码），兑现 API 文档 §6.9 的既有承诺——泄露后复盘要找的正是这些探针。审计 detail 不含新明文（与成功路径一致）。
- **JWT 从 RS256 换 EdDSA（Ed25519）**（[PR #37](https://github.com/NJUPT-SAST/sast-link-backend-v2/pull/37)）：JWKS 变 `kty=OKP/crv=Ed25519/alg=EdDSA`、discovery `id_token_signing_alg_values_supported=["EdDSA"]`、ID Token 同算法；密钥解析改 PKCS8，部署需换 Ed25519 密钥；验签 leeway 缩到 5s。
- **auth-state 缓存取代 JTI 黑名单**（[PR #37](https://github.com/NJUPT-SAST/sast-link-backend-v2/pull/37)）：中间件把 DB 权威的撤销 / 角色状态按 JTI 短 TTL 缓存（`AUTH_STATE_CACHE_TTL`，默认 15s），撤销路径写短 TTL tombstone（非 DEL）且缓存回填用 SET NX——关死撤销写竞态，撤销后旧角色无法通过竞态复活；缓存故障回落 DB（fail-open）。
- **refresh grace period（30s）**（[PR #37](https://github.com/NJUPT-SAST/sast-link-backend-v2/pull/37)）：并发刷新不再撤销整个 token family，双 tab 刷新不登出；超窗口的真 replay 才切族。
- **新哈希统一 argon2id**（[PR #37](https://github.com/NJUPT-SAST/sast-link-backend-v2/pull/37)）：默认 m=19456KiB / t=2（OWASP 低内存底线），存量 `pbkdf2-sha512-v1` 只读验证、登录成功后 rehash（`ShouldRehash`）；派生内存上界 64MiB；并发默认 `GOMAXPROCS`（1c1g=1）而非固定 64。取舍：login 因此是刻意保留的 CPU 热点，校园流量以读为主，读路径不受影响。
- **刷新轮换审计并入同一事务**（[PR #37](https://github.com/NJUPT-SAST/sast-link-backend-v2/pull/37)）：`audit_logs` 直写，消除审计与轮换之间的窗口。
- **GORM `PrepareStmt` 与连接池**（[PR #37](https://github.com/NJUPT-SAST/sast-link-backend-v2/pull/37)）：热查询缓存 PREPARE（MaxSize 128 / TTL 5m）；连接池 maxOpen 10 / maxIdle 3 / idle 5m / lifetime 30m。
- **运行时**（[PR #37](https://github.com/NJUPT-SAST/sast-link-backend-v2/pull/37)）：`automaxprocs` 按容器 cgroup 配额校准 `GOMAXPROCS`、采集 `cmd/api/default.pgo` 跑 PGO、server 补 ReadTimeout 30s / WriteTimeout 10s / IdleTimeout 60s、`GOMEMLIMIT=900MiB`、gin 非 development 自动 release、pprof 改 fail-closed（仅 development 或 `PPROF_ENABLED=true`）。
- **Redis 参数**（[PR #37](https://github.com/NJUPT-SAST/sast-link-backend-v2/pull/37)）：MaxRetries=0、MinIdleConns=4、拨号 / 读写短超时，Redis 挂时快速失败而不是挂住请求。
- **限流按校园 NAT 放宽**（[PR #37](https://github.com/NJUPT-SAST/sast-link-backend-v2/pull/37)）：per-IP 总值提高但保留有意义的节流（login →500/15m、token/refresh/authorize/oauth-login/exchange-code →300/min、send-code IP →100/min 等，数值在 [PR #47](https://github.com/NJUPT-SAST/sast-link-backend-v2/pull/47) 再次调高），登录防御回到按账号锁定（`LOGIN_FAILURE_LIMIT`）。
- **blacklist worker**（[PR #37](https://github.com/NJUPT-SAST/sast-link-backend-v2/pull/37)）：一次性 pipeline 失效一批 auth-state key，空队列按退避间隔休眠。
- **包布局按职责重构**（2026-07-26，[PR #24](https://github.com/NJUPT-SAST/sast-link-backend-v2/pull/24)）；LF 行尾统一。
- **OAuth 加固**（2026-07-30，[PR #28](https://github.com/NJUPT-SAST/sast-link-backend-v2/pull/28)）：`state` 逐字节返回、错误体不回显请求文本、token 认证失败不泄露客户端存在性 / 类型、discovery 不再宣称不支持的 `auth_time`、过期 access token 撤销其所属 family、`JWT_ISSUER` 规范化保证 `iss` 与 discovery 一致、loopback host 按 ASCII 折叠。
- **第一方客户端的注册 scope 约束**（[PR #44](https://github.com/NJUPT-SAST/sast-link-backend-v2/pull/44)，2026-08-11）：移除 `checkScopeForClient` 中 `first_party` 跳过 `ContainsAll` 的分支，两类客户端一律受注册 `scopes` 列约束。该豁免自 OAuth provider 首版（PR #28）起存在且从未有过论证，它使 `scopes` 列对第一方形同记录，且是追溯性的——将来新增任何 scope 都会被所有现存第一方注册立即获得，不产生授予动作或审计行。影响面为零：全库唯一的 `first_party` 注册是 V003 内置客户端，其 `scopes` 恰为 `openid profile email`（迁移带 drift 检测、`cmd/api` 启动时以 `scope.Equal` 校验），内部会话不走该路径。
- **admin scope 禁令的理由改为凭证能力**（[PR #44](https://github.com/NJUPT-SAST/sast-link-backend-v2/pull/44)，2026-08-11）：行为不变（仍仅 `third_party` 可持有），但原理由「第一方不受注册值约束故无能力上限」随上一条失效。改述为：`first_party` 是公开客户端，token 端点仅凭 PKCE 认证它（`authenticateClient` 按有无 secret 分支），从授权码被签出到管理 token 存在之间只剩 `redirect_uri` 精确匹配一道屏障，机密客户端有两道独立屏障。内置控制台本身是公开 `first_party` 且持有完整管理权，属既有例外——它靠一整套针对该注册的加固成立（`redirect_uris`/`scopes` 不可经 API 改、不可停用、启动校验、`client_id` 由 env 固定），这些加固对经 API 注册的其他第一方客户端一条都不适用，故不能据此推论公开客户端可持有管理权。
- **管理后台合同收紧**（2026-07-31，[PR #29](https://github.com/NJUPT-SAST/sast-link-backend-v2/pull/29)）：`page_size` 超上限直接 422 拒绝而非截断；管理端写的 `login_email` 拒绝不可见 codepoint。
- **资料编辑与解绑**（2026-07-28，[PR #27](https://github.com/NJUPT-SAST/sast-link-backend-v2/pull/27)）：profile 用 upsert 替代读后写；JSONB 修正为 JSON 序列化（之前会 base64）；解绑 cooldown 改为按用户限流。
- **容器以非特权用户运行**（2026-08-01，[PR #31](https://github.com/NJUPT-SAST/sast-link-backend-v2/pull/31)）。
- **OAuth 客户端更新契约放宽**（[PR #41](https://github.com/NJUPT-SAST/sast-link-backend-v2/pull/41)，2026-08-09）：`PUT /admin/oauth-clients/:id` 现可改 `grant_types` / `scopes`（此前这两字段不可修改，请求即 `400`），`client_id` / `client_secret` / `client_type` / `id` 仍不可改——`client_type` 决定客户端的凭据模型（有无 secret）与 scope 授予规则，就地翻转而不重发 secret 会产生无凭据的第三方客户端，换类型需重新注册。此类更新不触发 token 撤销，且只影响之后的新授权——存量 refresh token 轮换时按原授权 scope 继承，收窄注册 scope 不回溯已签发 token。

- **委派身份改由能力判定，拆除硬编码客户端名单**（[PR #44](https://github.com/NJUPT-SAST/sast-link-backend-v2/pull/44)，2026-08-10）：**破坏性变更**。删除 `model.AdminDelegatedClientID` 常量与 `Authenticator` 上对应字段，`AuthenticateAdminDelegated` 改为凭 token 携带的 admin scope 放行任何第三方客户端。该 scope 并非自证：`third_party` 的可请求 scope 被 `ContainsAll` 钉死在注册值内，而 `authorizeScopeForClient` 对 `first_party` 一律拒绝 admin scope，因此「token 持有 admin:read」已证明「运维人员为该注册授予过它」，名字校验只是对同一事实的第二次断言，代价是接入第二个运维工具需要一次迁移加一次发布。取舍是自觉的：提权原先需同时突破「注册表 scopes」与「azp 名单」两道，如今注册表是唯一屏障，足够提权的单点 bug 就是「把 admin scope 写到某个 third_party 注册上」——这正是上述四道守卫要防的，理由记在方法注释里，以免日后有人认为守卫多余。**影响面**：曾给某客户端授过 admin scope、却靠名字校验把它挡在 `/admin/*` 之外的部署，需从该注册移除这两个 scope。
- **OAuth 客户端 scope 变更的撤销语义**（[PR #44](https://github.com/NJUPT-SAST/sast-link-backend-v2/pull/44)，2026-08-10）：修正 [PR #41] 条目中「此类更新不触发 token 撤销，且只影响之后的新授权」的描述——能力收缩现在会回溯。`PUT /admin/oauth-clients/:id` 在三种情形下于同一事务内撤销该客户端全部 Access / Refresh Token：停用、**收窄 `scopes`**（旧集合中有值不在新集合里）、**新授予 admin scope**。收窄必须撤销，因为 access token 携带的是签发时的 scope，否则注册表已收窄而凭证仍在断言被收回的能力；新授予必须撤销，因为签发 token pair 时总会一并铸出 refresh 半边，被提升的客户端库里已有休眠的 refresh family。**扩大** `scopes` 刻意不撤销：存量 token 不会因此获得新 scope，撤销只是为一次不增加其权限的变更把用户登出。仅改 `grant_types` 亦不撤销。审计 detail 相应新增 `admin_scope_granted` / `admin_scope_revoked` / `scopes_removed`，并补上此前漏记的 `grant_types` / `scopes` 两个 `changed_fields`。
- **内置客户端保护扩展至 `scopes`**（[PR #44](https://github.com/NJUPT-SAST/sast-link-backend-v2/pull/44)，2026-08-10）：`PUT /admin/oauth-clients/:id` 现拒绝修改 `INTERNAL_OAUTH_CLIENT_ID` 的 `scopes`（403）。`cmd/api` 启动时对其做 `scope.Equal` 断言，此前一次 `PUT` 即可造成延迟自毁：运行中的进程照常工作，下次重启直接拒绝引导，只能直连数据库恢复；叠加上述收窄撤销逻辑，还会一次性切断全体用户的内部会话。已持有能力 scope 的客户端同样受额外保护：不可改写 `redirect_uris`、只能由控制台维护；admin scope 允许 `refresh_token`——`/admin` 角色门从数据库行读取主体角色，刷新不拓宽谁能用它；停用仍允许，那是委派管理的 kill switch。
- **登出改为按 access_token 家族撤销，不再要求 refresh token**（2026-08-13，[PR #49](https://github.com/NJUPT-SAST/sast-link-backend-v2/pull/49)）：`POST /auth/logout` 的 body `refresh_token` 变为可选且被忽略，服务端撤销当前 access_token 所属的 refresh_token family。修复了 stale-tab 登出「报成功但什么也没撤销」的旧问题（旧契约要求 body refresh token 与 access token 家族匹配，陈旧 tab 拿新登录的 cookie/family 提交旧 token 时被拒）；死会话登出幂等化（access token 在中间件校验与撤销之间失效时清 cookie 并返回成功）。**权限面变化（自觉取舍）**：登出从「access+refresh 双凭证」降为「仅 access token」——被窃的 access token 在其 TTL（1h）内可以持续撤销受害者会话（登出 DoS），换取 stale-tab 修复与幂等登出；已写入 PRD §4.6 备案。

- **`user.state` 改为自动状态机 + 钉住 override + 清算批次**（2026-09-01，[PR #78](https://github.com/NJUPT-SAST/sast-link-backend-v2/pull/78)）：`njupter` / `on_sast` / `retired_sast` 此前完全由管理员手动维护，已与事实脱节（restore 硬编码回 `njupter`、建号默认 `retired_sast` 对在校生是错的）。现由规则推导，**单一事实源在 `internal/validate.DeriveState`**，不复制进 SQL 或 repository：学号前 2 位数字为入学年份（格式有保证），学年在东八区 9/1 切换（`time.FixedZone` 硬编码，不依赖容器 `TZ` / tzdata），入学满 4 学年一律 `retired_sast`（压过 role），否则在校 `lecturer` / `admin` → `on_sast`、在校 `freshman` / `member` → `njupter`；`is_deleted` 保持手动独立通道。V014 加 `user.state_manual`（迁移不刷值，由首个清算批次自然校准）：`PUT /admin/users/:id` 传 `state` 即写值**并钉住**，推导与批次一律跳过；新增 `state_auto=true`（与 `state` 互斥，同传 400）在同一事务内锁行重推并解除钉住。建号 / 校友审批 / 注册 / restore 五处写路径改为推导——其中 **restore 的语义变更**值得单列：原「钉住的行保留原值」不可实现（DELETE 已把 state 覆盖为 `is_deleted`，值不存在），故一律推导并解除钉住，需重新钉住者在恢复后再 PUT。retention worker 新增 `RecomputeDerivedState`（同锁同 tick，id 序 keyset 游标，游标跨 tick 保留以免大表尾部永久饿死）；**只改 state，不 bump `token_version`、不撤销会话**——state 除 `is_deleted` 外不参与任何鉴权判定，故为低风险改造。钉住标记随 `state` 一并出现在 admin 列表 / 详情 / 批量读（看不见钉住就无法判断该不该发 `state_auto`，见 #60 的对称口径）。**运维提示**：9/1 已过，2022 级在生产已随首个批次迁往 `retired_sast`，`by_state` 的一次性跳变非故障；留级 / 延毕 / 中途退社等例外需管理员钉住兜底。
- **概览 `incomplete_by_state` 口径回正**（2026-09-01，[PR #79](https://github.com/NJUPT-SAST/sast-link-backend-v2/pull/79)）：回退 [PR #78](https://github.com/NJUPT-SAST/sast-link-backend-v2/pull/78) 中一处口径放宽。该 PR 把 `incomplete_by_state` 从「未补全且 `state = njupter`」改为「未补全的全部活跃账户（按 state 分组）」，理由是自动机下在校讲师 / 管理员落在 `on_sast`，固定 `njupter` 会让逾期未归类的在校非学生从概览消失。这个理由是错的，且违背 [PR #60](https://github.com/NJUPT-SAST/sast-link-backend-v2/pull/60) 记载的设计：`incomplete_by_role` 显式带 `role NOT IN (lecturer, admin)`，注释与文档都写明「讲师 / 管理员视为组织内成员，不列为待跟进对象」——「消失」正是该设计想要的结果，而非缺陷。放宽 state 维度后，两个维度对「未补全」的定义分叉：一个未补全的在职讲师在角色饼图里被有意隐藏，在状态饼图里却被计入「未补全」扇区，同一概览页出现两个不相等的数字。而自动机使 `njupter` 恰好等于「在校 `freshman` / `member`」，旧口径**仍然精确对应它原本要表达的人群**，根本没有坏。故恢复 `state = njupter` 过滤，并把注释与三处文档改写为解释这层对称关系（不可只放宽一维）。顺带补一处测试缺口：原用例没有「未补全 + `retired_sast`」的账号，因此只验证了「排除 staff」而未验证「只认 njupter」，新账号同时把两维度集合本就不同这一点钉成 documented behavior（该账号计入角色维度、不计入状态维度）。减法守恒性两种口径下都成立，差别仅在谁被单列为待跟进对象。

### Removed

- **`GET /card/:id` 下线**（[PR #37](https://github.com/NJUPT-SAST/sast-link-backend-v2/pull/37)）：**破坏性变更**。路由不再注册，公开个人卡片端点暂不响应。顺序数字 ID 的公开 URL 等于开放全站成员名单枚举；重开后为 owner-only + 不可枚举标识，不会原样启用。handler / service / repository 代码保留待重设计，`docs/openapi.yaml` 中该路径标 `deprecated`。
- **OIDC `profile` URL claim 与 `OAUTH_CARD_BASE_URL` 配置**（[PR #37](https://github.com/NJUPT-SAST/sast-link-backend-v2/pull/37)）：**破坏性变更**。随卡片端点一并移除——claim 若保留即等于给任何第三方客户端一条读取卡片投影的旁路。`profile` scope 现在只产出 `name` / `picture` / `preferred_username` / `updated_at`；`claims_supported` 与 UserInfo / ID Token schema 已同步。
- **V008 接入方客户端 seed 迁移**（[PR #44](https://github.com/NJUPT-SAST/sast-link-backend-v2/pull/44)，2026-08-11）：删除 `000008_seed_ops_oauth_client`，接入方客户端（含委派管理）改为一律经控制台注册。原因是 seed 迁移与凭证轮换不相容：漂移检测把 `client_secret` 哈希纳入比对（不纳入则 `down`/`up` 会把轮换后的行重建成迁移里的旧 secret，让已作废凭证复活），因此生产库一旦轮换 secret，任何需要重跑 V008 的路径——`down` 后再 `up`、灾备重建、从生产 dump 恢复后跑迁移——都会 `RAISE EXCEPTION` 中止且无法绕过。委派授予本就是控制台操作（`checkCapabilityScopeGrant` 四道守卫 + 审计行），schema 层授予反而绕过了这套校验。该迁移从未在生产执行过，故直接删除而非追加一个 `down` 迁移。测试改为自行注册 fixture 客户端（`delegated-e2e-admin`），不再依赖 seed 存在。
- **pg_cron 清理方案**（2026-08-01，[PR #33](https://github.com/NJUPT-SAST/sast-link-backend-v2/pull/33)）：被进程内 retention worker 取代；不用 pg_cron，因为生产库未安装该扩展且测试镜像无法加载。

### Fixed

- x/net 升级修 CVE-2026-25680、x/text 升级修 GO-2026-5970（2026-07-22 / 07-27）。
- 认证基础设施修复（2026-07-22，[PR #22](https://github.com/NJUPT-SAST/sast-link-backend-v2/pull/22)）：refresh rotation 原子化、限流器溢出、hash decode、空 JWT key ID 拒绝。
- 注册 / 密码 / 邮箱绑定修复（2026-07-28，[PR #26](https://github.com/NJUPT-SAST/sast-link-backend-v2/pull/26)）：跨表邮箱唯一（`login_email` 不能同时是 other_mail 身份）、注册冲突错误映射到具体字段、错误验证码 / 被拒请求不再烧一次性 token、mailer 收件人校验防 header 注入、验证码 key 按 purpose 隔离、错误信息中文化。
- 管理后台修复（2026-07-31，[PR #29](https://github.com/NJUPT-SAST/sast-link-backend-v2/pull/29)）：最后一名活跃管理员在事务内用 `pg_advisory_xact_lock` 判定，不再从写前读。
- refresh 失败审计补录（2026-08-01，[PR #32](https://github.com/NJUPT-SAST/sast-link-backend-v2/pull/32)），此前只记成功轮换。
- 头像上传加固（2026-08-03，[PR #34](https://github.com/NJUPT-SAST/sast-link-backend-v2/pull/34)）：COS HTTP client 加往返超时、被拒文件名记诊断日志、内容审核结果用专用错误码。
- 第三方登录设备登记修复（[PR #41](https://github.com/NJUPT-SAST/sast-link-backend-v2/pull/41)，2026-08-09）：`oauthlogin.ExchangeCode` 登记设备用 `s.Clock.Now()`，而 production 组合根（`cmd/api/runtime.go`）未给 oauthlogin 注入 Clock——GitHub / Lark 登录每次登记设备都会 nil 解引用 panic。改为走 `now()`，未注入 Clock 时回退系统时钟；补了 `service.Clock = nil` 模拟 production 的回归测试。
- 管理审计 detail 文档与实现对齐（[PR #44](https://github.com/NJUPT-SAST/sast-link-backend-v2/pull/44)，2026-08-10）：PRD 与 `docs/psql-db-design.md` 长期把管理端审计 detail 记作 `{"target_user_id", "sub_action"}`，而 `adminclient` 从未写过这两个字段。改为实际写入的字段。该错误早于本次改动。
- 委派管理相关文档自相矛盾修正（[PR #44](https://github.com/NJUPT-SAST/sast-link-backend-v2/pull/44)，2026-08-10）：`docs/openapi.yaml` 同一 operation 的 `200` 块声称 scope 更新不撤销 token，与其 description 冲突；`403` 块漏列内置客户端的 scopes 拒绝与整个委派客户端分支；PRD §4.12 在正确描述四段之后又称注册表修改不回溯；`API文档.md` §5.2 / §5.3 的错误码表未列新增的 `invalid_scope` 拒绝。
- **`/user` 自助面审计 actor 归因补齐**（2026-08-12，[PR #47](https://github.com/NJUPT-SAST/sast-link-backend-v2/pull/47) 遗留缺口）：`user:*` 第三方 token 与内置控制台会话在 `/user` 面的写操作（`logout` / `change_password` / `update_profile` / `upload_avatar` / `oauth_bind` / `oauth_unbind` / `bind_email_send_code` / `logout_device`）此前全部记 `actor_client_id = NULL`，审计无法区分「用户经控制台操作」与「应用（如 SAST People）代用户操作」。现在 `sessionhandler` 把 token 的 `azp` 透传进 session service 输入，`audit` 路径按 azp 写 `actor_client_id`（空 azp 解析为内置客户端 id，对齐 adminclient 语义），未认证流程（login / refresh / register / reset-password / 验证码类）保持 NULL，V007「NULL = 无 OAuth 凭证授权」的契约在新开放的面上一并成立。

### Security

- 请求日志对 `code` / `state` / `code_challenge` / `registration_state` / `oauth_state` / `login_code` 脱敏（[PR #37](https://github.com/NJUPT-SAST/sast-link-backend-v2/pull/37)）。
- 忘记密码统一成功响应，不暴露账号存在性（2026-07-28，[PR #26](https://github.com/NJUPT-SAST/sast-link-backend-v2/pull/26)）。
- 第三方回调重定向按精确匹配白名单校验（2026-07-31，[PR #30](https://github.com/NJUPT-SAST/sast-link-backend-v2/pull/30)）。
- provider endpoint URL 常量标 `#nosec G101`（2026-08-03，[PR #35](https://github.com/NJUPT-SAST/sast-link-backend-v2/pull/35)）。
- **收回委派 scope 的时间窗口关闭**（[PR #44](https://github.com/NJUPT-SAST/sast-link-backend-v2/pull/44)，2026-08-10）：`Consent` 此前只对活注册重查客户端启用状态与 `redirect_uri`，`tokenByAuthorizationCode` 只重查 `grant_types`，两者均不重查 scope。因此收回一个客户端的 admin scope 后，收回前已暂存的授权请求仍能继续签出管理级授权码（直到 authorize-request TTL 耗尽），已签发未兑换的授权码仍能换出管理级 access token（直到 code TTL 耗尽）。两处补上与第一段同源的 scope 活查（非可重定向：请求发起时是合法的，是运维人员的变更使其失效，故告知用户重新发起而非把错误抛给无从处理的客户端）；code 兑换处的校验排在消费之后，使拒绝仍然烧掉该 code，不给窃得授权码者留下可无限试探的目标。配合上述存量 token 撤销，收回三个时间窗口全部关闭。
- **委派客户端的自我增殖闭环封堵**（[PR #44](https://github.com/NJUPT-SAST/sast-link-backend-v2/pull/44)，2026-08-10）：拆除硬编码名单后，若不加约束，持有 `admin:write` 的委派 token 可自行注册或提升另一个委派客户端、乃至重新启用运维人员刚停用的那个（即绕过 kill switch），使可达 `/admin/*` 的客户端集合脱离人工批准自行增长。`checkCapabilityScopeGrant` 与 `checkProtected` 均要求 actor 为内置控制台客户端。
- **拆包绕过封堵**（[PR #44](https://github.com/NJUPT-SAST/sast-link-backend-v2/pull/44)，2026-08-10）：`updateFields` 早于 `FindByID` 执行，只能看到本次提交的字段，因此任何只校验入参的守卫都可两步绕过——先授能力 scope、再单独改某个兄弟字段。所有守卫改为在加载存量行之后、基于合并状态判定。后续在 [PR #47](https://github.com/NJUPT-SAST/sast-link-backend-v2/pull/47) 放开 admin scope 的 `refresh_token`（`/admin` 角色门实时读主体角色），原「admin 禁 refresh」前提下的拆包动机随之消失。
- 同意页防伪造应用名（[PR #41](https://github.com/NJUPT-SAST/sast-link-backend-v2/pull/41)，2026-08-09）：consent URL 上的 `client_name` / `scope` 可被伪造——攻击者可构造带自己合法 `request_id`、却显示可信应用名的同意页链接，诱导受害者授权给恶意应用。新增 `GET /oauth/authorize/consent` 从暂存返回服务端校验过的元数据，同意页改从该端点渲染，不再信任 URL 值。
